---
name: sdd-lang-<STACK>
description: >-
  How a test is actually written in THIS repo, per seam: where it goes, what it
  is named, which framework and assertion style, and the exact command that runs
  one single test. Read by /sdd-spec and /sdd-implement before any test is
  written. Generated by /sdd-init from this repo — not a stock template.
---

# Writing a test in this repo — <STACK>

Every claim below has to come from a file in this repo, and the file is named so
the next reader can check it. A plausible-looking convention nobody verified is
worse than an admitted gap: the loop follows it, produces tests that don't match
the suite, and the mismatch surfaces as a red that never goes green.

## The suite as it stands

- Test projects / packages: <paths, e.g. tests/Orders.Tests/>
- Frameworks in use: <e.g. xunit 2.6 + FluentAssertions — from the manifest, named>
- Total test files: <n>, from `probe-test-seams.sh`

## Commands

The single-test command is the important one. The red and green observations are
the evidence this plugin produces, and a suite-wide run does not provide them:
a test that was never run in isolation can be green because something else in the
suite set the state up.

| What | Command |
|---|---|
| One test by name | `<exact command, with the placeholder shown>` |
| One test file | `<...>` |
| The package's suite | `<...>` |
| Everything | `<...>` |

Verified by running: <which of the above was actually executed while writing
this file, and what it printed. An unrun command in this table is a guess.>

## Per seam

One section per seam name in `.claude/sdd-tdd/seams.json`, because those names
are the `Level` values the use-case tables are allowed to use.

### <seam name>

- Where the file goes: <path convention>
- Naming: <file and test-method naming, from a real example>
- How it is written: <the framework idiom, in two or three lines>
- Example in this repo: <path:line>
- How this seam fails: <what a failing assertion looks like — the loop needs to
  recognise a real red, not a compile error or a skipped test>

## Fixtures, doubles and data

- <how dependencies are faked here: the library, and one real example path>
- <the shared fixture/builder helpers a new test is expected to reuse>

## What this repo does NOT have

- <seams absent from the profile, and what introducing one would cost>
- <anything the loop must not assume: no snapshot tooling, no DB in CI, …>

## Gaps

- <every claim above that could not be verified against a file, stated plainly>
